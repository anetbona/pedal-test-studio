# system / edytory
.DS_Store
Icon?
desktop.ini
*.log

# Claude Code (konfiguracja lokalna)
.claude/

# dane generowane / runtime
app/data/events.ndjson
# uwaga: audio (app/public/audio/) JEST w repo — bez niego demo na Vercelu nie ma dźwięku

# na wszelki wypadek (projekt jest zero-dependency)
node_modules/
.env
